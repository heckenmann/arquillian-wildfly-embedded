<?xml version="1.0"?>
<arquillian xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://jboss.org/schema/arquillian" xsi:schemaLocation="http://jboss.org/schema/arquillian http://www.jboss.org/schema/arquillian/arquillian_1_0.xsd">

    <container qualifier="wildfly" default="true">
        <configuration>
            <!-- Dieser Eintrag kann verwendet werden, wenn der managed-Adapter verwendet wird (siehe pom.xml) und ein bereits lokal gestarteter Wildfly für die Tests verwendet werden soll. -->
            <!-- Ist kein Server gestartet, wird der im target-Verzeichnis gestartet. -->
            <property name="allowConnectingToRunningServer">true</property>
        </configuration>
    </container>
</arquillian>